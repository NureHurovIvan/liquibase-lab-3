<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="001-create-roles-table" author="Hurov">
        <createTable tableName="roles">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(50)">
                <constraints nullable="false" unique="true"/>
            </column>
        </createTable>

        <sql>
            INSERT INTO dbo.tables (db_, name, type, author, version, service, description, column_count, created_at)
            VALUES ('liquibase_lab', 'roles', 'DATA_TABLE', 'Hurov', 1, 'USER_SERVICE', 'Table for storing security roles',
                    (SELECT COUNT(*) FROM sys.columns WHERE object_id = OBJECT_ID('roles')), SYSUTCDATETIME());
        </sql>

        <rollback>
            <sql>DELETE FROM dbo.tables WHERE name = 'roles';</sql>
            <dropTable tableName="roles"/>
        </rollback>
    </changeSet>
</databaseChangeLog>
<changeSet id="001-create-category-table" author="lopatenko">
    <createTable tableName="category">
        <column name="id" type="BIGINT" autoIncrement="true">
            <constraints primaryKey="true" nullable="false"/>
        </column>
        <column name="name" type="VARCHAR(255)">
            <constraints unique="true" nullable="false"/>
        </column>
    </createTable>

    <createIndex indexName="idx_category_name" tableName="category">
        <column name="name"/>
    </createIndex>

    <sql>
        INSERT INTO dbo.tables (db, name, type, author, version, service, description, column_count, created_at, last_update_at)
        VALUES ('liquibase_lab', 'category', 'DATA_TABLE', 'lopatenko', 1, 'PRODUCT_SERVICE', 'Table for storing product categories.', 
        (SELECT COUNT(*) FROM sys.columns WHERE object_id = OBJECT_ID('category')), SYSUTCDATETIME(), NULL);
    </sql>

    <rollback>
        <sql>DELETE FROM dbo.tables WHERE name = 'category';</sql>
        <dropTable tableName="category"/>
    </rollback>
</changeSet>
<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="001-create-users-table" author="Hurov">
        <createTable tableName="users">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="login" type="VARCHAR(50)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="password" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="email" type="VARCHAR(100)"/>
            <column name="created_at" type="DATETIME" defaultValueComputed="GETDATE()"/>
        </createTable>

        <sql>
            INSERT INTO dbo.tables (db_, name, type, author, version, service, description, column_count, created_at)
            VALUES ('liquibase_lab', 'users', 'DATA_TABLE', 'Hurov', 1, 'USER_SERVICE', 'Main table for storing users',
                    (SELECT COUNT(*) FROM sys.columns WHERE object_id = OBJECT_ID('users')), SYSUTCDATETIME());
        </sql>

        <rollback>
            <sql>DELETE FROM dbo.tables WHERE name = 'users';</sql>
            <dropTable tableName="users"/>
        </rollback>
    </changeSet>
</databaseChangeLog>